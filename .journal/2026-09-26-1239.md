# 2026-09-26 12:39 +08 — Refyard cross-platform CI repair

## Core decision

Repair the real Refyard main-merge CI failures without narrowing the standalone effect set or relaxing Xross embedded authorization.

## Options considered

- Revert `with_writes` to three effects so old capability assertions pass. Rejected: current Refyard main implements the larger set, and its own Rust tests depend on it.
- Update stale assertions while investigating the status and SSE failures independently. Chosen because the status fixture and event transport had separate, observable root causes.
- Treat Windows failures as ignorable platform differences. Rejected: the embedded facade must retain approved-root bindings and report an initialized submodule's actual HEAD on all three CI platforms.

## Final decision and rationale

Keep the complete standalone write set and the explicit embedded subset. Wait for asynchronous fixture file writes before reading status. Suppress subscription events already sent by replay, since the subscription is created before the replay snapshot. On Windows, use Git's path representation for worktree ID lookup while retaining filesystem-canonical paths for containment, and canonicalize the child's reported top-level before the exact-submodule check. The latter remains fail-closed for an uninitialized directory.

## Key changes and evidence

- Updated native capability tests for Refyard main's expanded read/write surface and aligned the release metadata assertion with the current `0.2.0` package manifest.
- Awaited three fixture writes that previously raced immediate status reads.
- Added a monotonic live-event guard after SSE replay. GitHub CI had reproduced a backwards replay/live frame; the local native suites now pass.
- Local release build, 35 Rust HTTP tests, 18 embedded host tests, 1,168 TypeScript tests, workspace TypeScript checks, Rust formatting and strict targeted Clippy passed on macOS.
- Windows worktree/submodule corrections are source-traced and locally compiled/tested, but await the exact-head Windows CI rerun; this journal does not claim Windows pass.

## Future considerations

- The independent review found no P0/P1 in the SSE/TypeScript repair. Two P2 follow-ups were recorded in Xross `docs/review-followups.md`: externally supplied future SSE cursors and capability-test exhaustiveness.
- Recheck the complete Linux/macOS/Windows CI matrix after pushing. Do not pin Xross's Refyard vendor gitlink until that evidence is green.
