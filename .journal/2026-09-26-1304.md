# 2026-09-26 13:04 SGT — CI fixture ordering and Windows worktree identity

## Core decision

Treat the latest Linux shutdown and Windows embedded-worktree failures as test-fixture/representation defects, not as evidence to loosen the host's root or journal guards.

## Options considered

- Add sleeps before status reads: rejected; the fixture's `repo.write` is asynchronous, and a delay would only hide the missing await.
- Compare raw Git worktree display spelling to a canonical native path: rejected on Windows, where slash/case spelling can differ without changing the selected worktree.
- Change production worktree identity or authorization: rejected; the previous CI run already passed the binding and actual-submodule checks, and this failure was a final test assertion.

## Final decision and changes

Await both fixture writes in `http-shutdown.test.ts`. Compare the refreshed worktree's canonicalized display path to the expected canonical path in `embed.rs`, matching the earlier assertion in that same test. The Linux shutdown test now passes 2/2 locally; the targeted embedded-worktree test and Rust formatting pass. A parallel run of all 17 `embed` tests once hit an unrelated reopened-host state-lock race; the targeted affected test passed, and Windows CI remains the decisive cross-platform gate.

## Future considerations

Do not claim all-platform CI green until the pushed commit runs. If the state-lock race recurs, isolate the fixture's host lifetime rather than weakening the lock.
