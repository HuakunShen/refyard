# 2026-09-26 12:49 +08 — Correct the SSE CI diagnosis

## Core decision

Withdraw an unnecessary server-side SSE de-duplication change and test the actual replay ordering contract.

## Options considered

- Keep the de-duplication guard because it addresses a plausible subscribe/replay overlap. Rejected for this CI repair: the failing assertion was not about event sequence. A future cursor also needs a defined reset policy before the guard is safe for direct API callers.
- Make the test assert that the reconnect hint follows *every* `data:` frame. Rejected: live frames may arrive after the hint on an open stream.
- Separate replay frames from later live frames at the hint, then check the submitted operation is in replay and all frames after a resumed cursor are newer. Chosen.

## Final decision and rationale

The GitHub failure `expected 2662 to be greater than 3348` came from `indexOf("retry: 3000") > lastIndexOf("data: ")`: these were character offsets, not event sequence numbers. The earlier journal entry at 12:39 incorrectly attributed that particular failure to duplicate event delivery. The source-level overlap remains a separate P2 design item; it is not part of this repair.

## Key changes and evidence

- Removed the guard from `refyard-http` introduced in the prior commit.
- Updated the native stream test to parse only the frames preceding the reconnect hint as replay; a caught-up subscription waits for the hint anywhere and validates event IDs against its cursor.
- On Windows, the remaining embed failure was an assertion comparing Git's `C:/...` display spelling with a filesystem-canonical path. The root-binding assertions now pass; the external worktree's display path is checked by resolving it to the same physical path. The submodule OID assertion also passed in that CI attempt.
- Local Rust embed tests and targeted native HTTP event tests pass; Linux/macOS/Windows CI must still verify the new exact commit.

## Future considerations

- Define a session-bound SSE cursor and safe future-cursor reset, then add deterministic overlap tests before any de-duplication policy is reintroduced. This is recorded as P2 in Xross `docs/review-followups.md`.
