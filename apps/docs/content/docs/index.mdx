---
title: What Refyard is
description: A local-first Git workbench that drives your machine's own git and never asks for a hosted copy of your repository.
---

import { Cards, Card } from "fumadocs-ui/components/card";
import { Image } from "astro:assets";
import workbenchDark from "../../src/assets/workbench-dark.png";
import commitDetailDark from "../../src/assets/commit-detail-dark.png";
import pullRequestsDark from "../../src/assets/pull-requests-dark.png";

Refyard is a Git workbench that runs **on your machine, against your repositories, with
your `git`**. It opens the repository where it already is — with your hooks, your filters,
your credential helpers and your SSH configuration — and gives it a graph you can read, a
working copy you can stage and commit from, and panels for branches, remotes, stashes, tags,
worktrees and submodules.

It is not a hosted service. There is no account, no upload step, and no central copy of your
code. The one thing it fetches over the network is GitHub profile pictures for the author
column, and that is a setting you can turn off.

<Image
  src={workbenchDark}
  alt="The Refyard workbench: a merge-dense commit graph with lane lines and author avatars, the working-copy pane, and the commit composer"
  width={1600}
  loading="eager"
  class="rounded-lg border border-fd-border overflow-hidden"
/>

*The history graph of a real repository — the host machine's own `git` reads it, nothing
is uploaded.*

<Cards>
  <Card title="Your git, not ours">
    Refyard builds Git commands and runs them as you. Signing, hooks, filters and
    `~/.ssh/config` keep working because it is the same `git` your terminal uses.
  </Card>
  <Card title="Local by default">
    The browser form listens on loopback only, authenticates every read as well as every
    write, and hands out access with single-use pairing tickets minted on trusted local
    channels.
  </Card>
  <Card title="One UI, several forms">
    A native desktop app, a local browser session, an optional hosted UI, and a VS Code
    webview — all rendering the same components against the same contract.
  </Card>
  <Card title="Honest about gaps">
    If a form cannot do something, it says so instead of pretending. Unknown outcomes are
    reported as unknown, never as success.
  </Card>
</Cards>

## A closer look

<Image
  src={commitDetailDark}
  alt="A selected commit: split diff of one file beside the commit's metadata — author avatar, signature, refs"
  width={1600}
  class="rounded-lg border border-fd-border overflow-hidden"
/>

*Selecting a commit opens its diff in the main workspace; patches are read one file at a
time against the host's `git`.*

<Image
  src={pullRequestsDark}
  alt="The pull-requests panel connected to GitHub, listing open pull requests with author avatars next to the graph"
  width={1600}
  class="rounded-lg border border-fd-border overflow-hidden"
/>

*The GitHub panel: connect once and open pull requests appear next to the history — the
token stays on the machine, the browser never sees it.*

## Where to go next

- **[Install](/install/)** — the desktop app, or `npx` for the CLI.
- **[First session](/first-session/)** — open a repository and pair a browser.
- **[History and the graph](/workbench/history/)** — what the main view shows.
- **[What Refyard will not do](/security/)** — the safety rules, stated plainly.
