[toolchain]
# The channel, not a pinned version: the release matrix cross-compiles from one runner
# to another target (mac x64 builds on an arm64 runner), and rust-toolchain must agree
# with the `stable` toolchain the workflow installs its cross targets into.
channel = "stable"
components = ["rustfmt", "clippy"]
