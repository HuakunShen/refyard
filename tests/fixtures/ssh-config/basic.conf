# The plan's own example. A Host line may carry several concrete patterns; a line may
# mix a concrete pattern with rules that are not machines; and a Host with no options at
# all still names a candidate, because a person who wrote it means to connect to it.
Host prod staging
Host *.corp !blocked
Host bare
