# Staged as the primary config in a directory the test chooses, while sibling.conf lives
# in include_base. `sibling.conf` is relative, so it must resolve against include_base —
# OpenSSH's rule — and not against this file's own directory.
Host relative-root
Include sibling.conf
