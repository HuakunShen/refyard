# Staged as the primary config. The Include sits inside a Match block, which OpenSSH
# reads only when it evaluates that match, so the walker must report it instead of
# reading inside-match.conf as if it were unconditional.
Host listed-before
Match host web1
    Include inside-match.conf
Host listed-after
